---
alwaysApply: true
---
TDD + ownership rules for Cursor agents

You MUST follow folder ownership. Only edit files inside your assigned folder.

TDD is required:

Write or update tests FIRST.

Run tests (or reason about how they run).

Implement minimal code to pass tests.

Refactor (optional).

Every module must have tests.

External dependencies MUST be mocked in unit tests:

OpenAI calls

Weaviate calls

Network calls

No “integration” calls in unit tests.

Each service must include:

README with run commands

.env.example

health endpoint or equivalent

Definition of Done for any PR:

tests added

tests pass

types/validation included (pydantic)

logs included for key steps